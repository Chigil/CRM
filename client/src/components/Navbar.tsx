import React, {useState} from 'react';

const Navbar: React.FC = () => {

    return (
        <div>
            <nav>
                <div className="nav-wrapper red darken-4 px1">
                    <a className="brand-logo">CRM USERS</a>
                </div>
            </nav>
        </div>
    );
};

export default Navbar;